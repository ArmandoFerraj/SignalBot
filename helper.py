

def process_message(message): #classifies each element as a trading signal or not, returns a list of all trading signals

    """
    - this function takes the discord string posted to the Server as input
    - we want to parse this string and create a well defined trading signal that our bot can use
    - trading signal e.g.  
    
    {symbol:'BTC', enter_price:1.20, exit_price:1.26}
    - """
    
    #insert logic to parse "message" and use that info to create a trading signal

  
    return(message)
                  





